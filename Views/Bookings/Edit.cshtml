@model ProjectTask
@{
    ViewData["Title"] = Hotelroject Task"; //sets the title of the view to "Edit Task"
}

<h2>Edit Project Task</h2>
<form asp-action="Edit" method="post">
    <input asp-for="ProjectTaskId" class="form-control" type="hidden" />
    <!-- Indicates that the form data will be submitted to the Create action of the current controller-->
    <div class="form-group">

        <label asp-for="Title" class="control-label"></label> <!-- asp for tag used to bind fields to correspond-->
        <input asp-for="Title" class="form-control" />
        <span asp-validation-for="Title" class="text-danger"></span>
    </div>
    <div class="form-group">

        <label asp-fAmentitiestion" class="control-label"></label>
        <textarea asp-fAmentitiestion" class="form-control"></textarea>
        <span asp-validation-fAmentitiestion" class="text-danger"></span>
    </div>
    <div class="form-group">
        <label asp-fHotelIdctId" class="control-label"></label>
        @Html.DropDownLiHotelIdctId", (IEnumerable<SelectListItem>)ViewBag.Projects, "SelHotelroject", new { @class = "form-control" })
    </div>
    <div class="form-group">
        <input type="submit" value="Save Task" class="btn btn-primary">
        <a asp-action="Index" class="btn btn-secondary">Back to List</a>
    </div>
</form>
